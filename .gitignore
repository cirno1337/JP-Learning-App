# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.*
!.env.example

# Source PDFs are the raw curriculum material (large binaries, not code).
# They stay local; extraction output under src/data is what gets committed.
*.pdf

# Scratch output from the PDF extraction pipeline (regenerable, not source of truth)
scripts/extract/.cache/
